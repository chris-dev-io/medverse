{
  "name": "medverse",
  "version": "1.0.0",
  "private": true,
  "description": "Medverse Firebase Server",
  "scripts": {
    "dev": "npm --prefix functions run dev",
    "serve": "npm --prefix functions run serve",
    "build": "npm --prefix functions run build",
    "test": "npm --prefix functions test",
    "test:watch": "npm --prefix functions run test:watch",
    "test:firestore": "npm --prefix functions run test:firestore",
    "test:emulators": "npm --prefix functions run test:emulators",
    "deploy": "npm --prefix functions run deploy"
  },
  "devDependencies": {
    "firebase-tools": "^15.2.1"
  }
}

